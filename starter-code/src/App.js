import React from "react";

function App() {
  return (
    <div className="App">
      <Switch>
        <Route exact path="/" component={} />
      </Switch>
    </div>
  );
}

export default App;
// mettre ici le switch pour le cas / tu render homepage etc
